Generado con la herramienta integrada image_gen (sin CLI).

Use case: infographic-diagram. Crea una imagen educativa en español de alta resolución, horizontal, limpia y muy legible, titulada "Turismo Valencia RAG · Arquitectura del proyecto". Para una persona principiante. Fondo claro, cajas grandes, flechas claras, iconos discretos de documentos, base de datos y ordenador. Tres franjas y pie de apoyo, sin cruces de flechas confusos.
Franja 1 azul: "1. PREPARAR LOS DOCUMENTOS · turismo-rag index". Flujo izquierda a derecha: "Documentos .txt / data/Documentos Fuente" → "ingestion.py / Lee texto y procedencia" → "chunking.py / Fragmentos de hasta 1.000 caracteres / Solapamiento: 150" → "embeddings.py / Ollama + EmbeddingGemma / Texto → vectores" → "ChromaDB / data/vector_store / Texto + vectores + fuente". Encima del flujo indica "indexing.py coordina la preparación". Rama de chunking hacia pequeña caja "data/processed/chunks.jsonl".
Franja 2 verde: "2. RESPONDER PREGUNTAS · turismo-rag serve". Flujo: "Usuario / Navegador: /docs" → "api.py / FastAPI + Uvicorn / POST /ask" → "pipeline.py / TourismAgent coordina". Desde este coordinador el recorrido continúa con tres cajas: "retrieval.py / Vectoriza la pregunta / Busca en ChromaDB" → "generation.py / Pregunta + fragmentos / LangChain → Ollama + Gemma 3" → "Respuesta JSON / Texto + fuentes + citas". Conexión claramente etiquetada desde ChromaDB de la franja superior hasta retrieval.py "Fragmentos recuperados". Etiqueta pequeña debajo de retrieval: "Usa embeddings.py + EmbeddingGemma". Indicar al lado de respuesta "La API la devuelve al usuario". No implicar que el usuario consulta directamente Ollama.
Franja 3 naranja: "3. EVALUAR · turismo-rag evaluate". "data/dataset.csv / Preguntas y referencias" → "evaluation.py / Prueba búsqueda y respuestas" → "data/evaluation / Resultados y métricas". Nota legible "El CSV sirve para evaluar; no se incorpora al índice".
Pie gris: "main.py: comandos de terminal | config.py + .env: ajustes | schemas.py: formatos y validación | tests/test_rag.py: pruebas | pyproject.toml: instalación y dependencias".
Nota final: "Aplicación Python modular y local. Flujo RAG fijo, sin memoria de conversación. Ollama ejecuta dos modelos: uno para vectores y otro para redactar." Mantener nombres técnicos exactos, no inventar servicios cloud, Docker, agentes adicionales o conexiones. El dibujo representa estructura lógica, no microservicios.
